{
  "name": "coarse-correct",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "rm -rf www && mkdir -p www/css www/js www/icons && cp index.html journal.html favicon.svg manifest.json www/ && cp css/* www/css/ && cp js/* www/js/ && cp icons/* www/icons/",
    "sync": "npm run build && npx cap sync android",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "http://local_proxy@127.0.0.1:43821/git/adam-rapoport/coarse-correct"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0"
  }
}
